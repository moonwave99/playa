<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="generator" content="goffre" />
  <meta name="viewport" content="width=device-width,minimum-scale=1.0,initial-scale=1,user-scalable=yes" />
  <title>{{title}} | {{config.defaultTitle}}</title>
  {{> meta}}
  <link rel="stylesheet" href="{{ getUrl 'bundle.css' }}">
  </link>
</head>

<body class="w-100 sans-serif page-{{slug}}"></body>
  {{>nav}}
  <main class="main w-100">
    {{{body}}}
  </main>
  {{>footer}}
  <script src="{{ getUrl 'bundle.js' }}"></script>
  <script>
    init({
      domain: '{{ options.domain }}'
    });
  </script>
</body>

</html>